package validation

import "testing"

func Test(t *testing.T) {
	t.Run("", func(t *testing.T) {
		// Att value inte valideras ifall det inte har uppdaterats

		// Validate
		// ValidateChangeStep
	})
}
